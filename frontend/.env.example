NEXT_PUBLIC_API_URL=http://localhost:8000
# Optional: direct read-only dashboard queries against the shared Postgres.
# The backend API is the primary path; use this only for heavy aggregations.
# The docker stack exposes Postgres on host port 5433.
DATABASE_URL=postgresql://gitly:gitly@localhost:5433/gitly
