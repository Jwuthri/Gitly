# Alembic config — minimal on purpose. The DB URL comes from gitly settings (env/.env)
# in alembic/env.py, never from this file. Run: alembic upgrade head
[alembic]
script_location = alembic
prepend_sys_path = .
